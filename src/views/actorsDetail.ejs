<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= actor.first_name %> <%= actor.last_name %></title>
    <link rel='stylesheet' href='/css/style.css' />

</head>
<body>
    <figure>
        <img src="/img/logo-DH.png" alt="Logo Digital House">
    </figure>
    <h1><%= actor.first_name %> <%= actor.last_name %></h1>    
    <br>
    <p>PRIMER NOMBRE: <%= actor.first_name %> </p>
    <p>SEGUNDO NOMBRE: <%= actor.last_name %></p>
    <p>RATING: <%= actor.rating %> </p>
    <p>
        PELICULA FAVORITA:
    <% for(let i = 0; i < movies.length; i++) { %>
         <%= movies[i].id == actor.favorite_movie_id ? movies[i].title : null %>  </p>
    <% } %>
    <br>
    <section>
        <a href="/actors/edit/<%= actor.id %> ">
            <button class="botonAgregar">Modicar</button>
        </a>
        <a href="/actors/delete/<%= actor.id %> ">
            <button class="botonBorrar">Eliminar</button>
        </a>
    </section>

</body>
</html>